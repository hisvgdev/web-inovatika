'use client'

import RestorePass from '@/components/resotre-pass'
import { Suspense } from 'react'

export default function RestorePassPage() {
    return (
        <Suspense>
            <RestorePass />
        </Suspense>
    )
}
